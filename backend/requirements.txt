# PaperLens backend dependencies

# Web framework
fastapi==0.111.1
pydantic==2.7.4
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.31
psycopg2-binary==2.9.9

# LangChain / LangGraph — all pinned to compatible 0.3.x series
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0
langgraph>=0.2.0,<0.3.0
msgpack>=1.0.8
langchain-anthropic>=0.2.0,<0.4.0
langchain-openai>=0.2.0,<0.4.0
langchain-google-genai>=2.0.0,<3.0.0
langchain-mistralai>=0.2.0,<0.4.0
langchain-groq>=0.2.0,<0.4.0
langsmith>=0.1.17,<0.3.0

# PDF parsing
PyMuPDF==1.24.7
pdfplumber==0.11.2

# HTTP client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
tenacity==8.4.2

# Vector store + embeddings
# Using newer chromadb (prebuilt wheel, no C++ build needed)
# sentence-transformers removed — too heavy for free tier (pulls torch ~2GB)
# Using chromadb's default embedding function instead
chromadb==0.6.3

# Auth
pyjwt==2.8.0
passlib==1.7.4
bcrypt==4.1.2

# Local model support
ollama==0.3.0

# Rate limiting
slowapi==0.1.9

# Testing
pytest==8.2.2
